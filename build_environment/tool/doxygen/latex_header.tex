% Minimal LaTeX header for clean CV output
\documentclass[10pt,a4paper]{article}

% Essential packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}                    % Times New Roman font
\usepackage[english]{babel}
\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{array}

% Ultra-compact page geometry
\geometry{
    a4paper,
    top=1.2cm,
    bottom=1.0cm,
    left=1.5cm,
    right=1.5cm,
    headheight=0cm,
    headsep=0cm,
    footskip=0.2cm
}

% Remove headers and footers
\pagestyle{empty}

% Hyperlink setup (no colored links to match Word)
\hypersetup{
    colorlinks=false,
    pdfborder={0 0 0}
}

% Section formatting to match centered bold headers
\titleformat{\section}
{\centering\normalfont\large\bfseries}
{}{0em}{}
[\vspace{-0.3em}]

\titleformat{\subsection}
{\normalfont\normalsize\bfseries}
{}{0em}{}

% Ultra-compact custom commands
\newcommand{\cvheader}[1]{
    \begin{center}
        {\fontsize{14pt}{16pt}\selectfont\textbf{#1}}
    \end{center}
    \vspace{-0.4em}
}

\newcommand{\cvcontact}[1]{
    \begin{center}
        \rule{\textwidth}{0.4pt}\\
        \vspace{0.01em}
        {\small #1}
    \end{center}
    \vspace{0.1em}
}

\newcommand{\cvsection}[1]{
    \vspace{0.1em}
    \begin{center}
        \textbf{\normalsize #1}
    \end{center}
    \vspace{0.02em}
}

\newcommand{\cventry}[4]{
    \noindent
    \begin{tabularx}{\textwidth}{@{}X@{}r@{}}
        \textbf{#1} & \textbf{#2} \\
        {#3} & \\
    \end{tabularx}
    {#4}
    \vspace{0.1em}
}

\newcommand{\cvskill}[2]{
    \textbf{#1}: #2\\
}

% Less compact list formatting
\setlist[itemize]{
    leftmargin=0.9em,
    itemsep=-1pt,
    parsep=0pt,
    topsep=2pt,
    partopsep=0pt
}

% Less compact paragraph settings
\setlength{\parindent}{0pt}
\setlength{\parskip}{3pt}

% Balanced line spacing and better text flow
\renewcommand{\baselinestretch}{0.92}
\tolerance=1000
\emergencystretch=2em
\hyphenpenalty=10000
\exhyphenpenalty=100

% Define all Doxygen commands with minimal section spacing
\newcommand{\doxysection}[1]{\vspace{0.05em}\begin{center}{\fontsize{14pt}{16pt}\selectfont\textbf{#1}}\end{center}\vspace{0.02em}}
\newcommand{\doxysubsection}[1]{\vspace{0.1em}\begin{center}\textbf{\normalsize #1}\end{center}\vspace{0.02em}}
\newcommand{\doxysubsubsection}[1]{\vspace{0.1em}\textbf{#1}\vspace{0.02em}}
\newcommand{\DoxyHorRuler}[1]{\vspace{0.02em}\noindent\rule{\textwidth}{0.4pt}\vspace{0.02em}}
\newenvironment{DoxyEnumerate}{\begin{enumerate}}{\end{enumerate}}
\newenvironment{DoxyItemize}{\begin{itemize}}{\end{itemize}}

% Define Doxygen line break command
\newcommand{\+}{}

% Define missing commands that might not exist
\providecommand{\printindex}{}
\providecommand{\clearemptydoublepage}{\clearpage}

% Override Doxygen document structure commands to remove front matter
\renewcommand{\tableofcontents}{}
\renewcommand{\listoffigures}{}
\renewcommand{\listoftables}{}
\renewcommand{\printindex}{}
\renewcommand{\clearemptydoublepage}{}

% Start document immediately with content
\begin{document}
