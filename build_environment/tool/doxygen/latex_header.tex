% Minimal LaTeX header for clean CV output
\documentclass[10pt,a4paper]{article}

% Set smaller font size globally
\renewcommand{\normalsize}{\fontsize{9pt}{10.8pt}\selectfont}
\renewcommand{\small}{\fontsize{8pt}{9.6pt}\selectfont}
\renewcommand{\footnotesize}{\fontsize{7pt}{8.4pt}\selectfont}
\renewcommand{\large}{\fontsize{11pt}{13.2pt}\selectfont}

% Essential packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}                    % Times New Roman font
\usepackage[english]{babel}
\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{array}
\usepackage{microtype}                % Better typography and spacing

% Ultra-compact page geometry
\geometry{
    a4paper,
    top=0.8cm,
    bottom=0.6cm,
    left=1.0cm,
    right=1.0cm,
    headheight=0cm,
    headsep=0cm,
    footskip=0.1cm
}

% Remove headers and footers
\pagestyle{empty}

% Hyperlink setup (no colored links to match Word)
\hypersetup{
    colorlinks=false,
    pdfborder={0 0 0}
}

% Section formatting to match centered bold headers
\titleformat{\section}
{\centering\normalfont\large\bfseries}
{}{0em}{}
[\vspace{-0.5em}]

\titleformat{\subsection}
{\normalfont\normalsize\bfseries}
{}{0em}{}
[\vspace{-0.2em}]

% Ultra-compact custom commands
\newcommand{\cvheader}[1]{
    \begin{center}
        {\fontsize{13pt}{14pt}\selectfont\textbf{#1}}
    \end{center}
    \vspace{-0.6em}
}

\newcommand{\cvcontact}[1]{
    \begin{center}
        \rule{\textwidth}{0.4pt}\\
        \vspace{-0.02em}
        {\small #1}
    \end{center}
    \vspace{-0.1em}
}

\newcommand{\cvsection}[1]{
    \vspace{0.05em}
    \begin{center}
        \textbf{\normalsize #1}
    \end{center}
    \vspace{-0.05em}
}

\newcommand{\cventry}[4]{
    \noindent
    \begin{tabularx}{\textwidth}{@{}X@{}r@{}}
        \textbf{#1} & \textbf{#2} \\
        {#3} & \\
    \end{tabularx}
    {#4}
    \vspace{0.05em}
}

\newcommand{\cvskill}[2]{
    \textbf{#1}: #2\\
}

% Ultra-compact list formatting
\setlist[itemize]{
    leftmargin=0.6em,
    itemsep=0pt,
    parsep=0pt,
    topsep=1pt,
    partopsep=0pt,
    before=\vspace{-0.2em},
    after=\vspace{-0.1em}
}

% Minimized paragraph settings for compactness
\setlength{\parindent}{0pt}
\setlength{\parskip}{2pt}

% Tighter line spacing for maximum density
\renewcommand{\baselinestretch}{0.85}
\tolerance=1000
\emergencystretch=1em
\hyphenpenalty=10000
\exhyphenpenalty=100

% Enable microtype for better character spacing
\microtypesetup{
    expansion=true,
    tracking=true,
    kerning=true,
    spacing=true
}

% Slightly tighter character spacing for more content
\SetTracking{encoding=*}{-10}

% Define all Doxygen commands with ultra-tight section spacing
\newcommand{\doxysection}[1]{\vspace{0.05em}\begin{center}{\fontsize{13pt}{14pt}\selectfont\textbf{#1}}\end{center}\vspace{0.02em}}
\newcommand{\doxysubsection}[1]{\vspace{0.08em}\begin{center}\textbf{\normalsize #1}\end{center}\vspace{0.02em}}
\newcommand{\doxysubsubsection}[1]{\vspace{0.08em}\textbf{#1}\vspace{0.02em}}
\newcommand{\DoxyHorRuler}[1]{\vspace{-0.02em}\noindent\rule{\textwidth}{0.4pt}\vspace{-0.02em}}
\newenvironment{DoxyEnumerate}{\begin{enumerate}}{\end{enumerate}}
\newenvironment{DoxyItemize}{\begin{itemize}}{\end{itemize}}

% Define Doxygen line break command
\newcommand{\+}{}

% Define missing commands that might not exist
\providecommand{\printindex}{}
\providecommand{\clearemptydoublepage}{\clearpage}

% Override Doxygen document structure commands to remove front matter
\renewcommand{\tableofcontents}{}
\renewcommand{\listoffigures}{}
\renewcommand{\listoftables}{}
\renewcommand{\printindex}{}
\renewcommand{\clearemptydoublepage}{}

% Start document immediately with content
\begin{document}
